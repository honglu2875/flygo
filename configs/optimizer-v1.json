{
  "name": "optimizer-v0-lr-screen-v1",
  "release": "v0-1m",
  "feature_cache": true,
  "updates": 1000,
  "batch_size": 32,
  "passes": 4,
  "groups": 656,
  "seeds": [
    1
  ],
  "rates": [
    0.0003,
    0.001,
    0.003,
    0.01,
    0.03,
    0.1
  ],
  "clip": 1,
  "eval_every": 100,
  "checkpoint_every": 500,
  "diagnostics_every": 100,
  "question": "Separate fixed-topology optimizer calibration on balanced V0; six rates, identical initialization and sampled training positions, equal exposures; no architecture or final strength selection.",
  "selection": "Report all rates including nonfinite failures; confirm best stable rates and 0.003 with two additional seeds and a longer equal budget before changing architectural conclusions.",
  "execution": "Assign free qualified CPU lanes or a numerically qualified TPU learner without changing batch size; record source and device timing."
}
